{
  "name": "conversekit-api",
  "version": "1.1.0",
  "description": "The ConverseKit API Worker — Hono over Supabase, all tiers, one deploy target",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:staging": "wrangler deploy --env staging",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.13.1",
    "jose": "^6.2.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250620.0",
    "typescript": "^5.8.3"
  }
}
